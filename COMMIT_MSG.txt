frontend: resync vendored guest-grammar.edn to authority (fs-browse-dir head)

Resyncs this repo's vendored copy of lang/guest-grammar.edn to the
kotoba-lang authority's fs-browse-dir head addition (capability id 261,
kotoba-lang/find recursive tree-walk), and advances the
guest_grammar_vendor_test digest pin to the new grammar sha256
(ee7ea37c). The paired kotoba-lang, grammar and kotoba pins follow in
their own PRs per the shared digest-carry rule.