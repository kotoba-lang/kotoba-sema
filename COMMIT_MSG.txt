catalog: register fs/browse-dir capability (wire id 261) in vendored copy

Adds :fs/browse-dir (source-op fs/browse-dir, effect :host/fs-browse-dir,
compiler-wire-id 261) to this repo's vendored copy of
lang/capability-catalog.edn after :fs/browse (34) — the family the
kotoba-lang/find recursive tree-walk lowers. Pairs with kotoba-lang
85fcca93 (authority) and kotoba-core-contracts 2ff3f736 (capability id 261).
This catalogue is what an amu-consuming compiler reads across its pin, so
the amu kotoba-sema pin must advance to this merge afterwards.